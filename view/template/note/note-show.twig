{% extends 'template/layout.twig' %}
{% block content %}
    <div class="container" style="width: 50%;">
        <section>
            <header>
                <h2>Note créée le {{ note.created_at }}</h2>
            </header>
            <div>
                <div class="groupe-info">
                    <small>Titre</small>
                    <span>{{ note.title }}</span>
                </div>
                <div class="groupe-info">
                    <small>Contenu</small>
                    <span>{{ note.content }}</span>
                </div>
            </div>
        </section>
    </div>
{% endblock %}
